import { StyleSheet } from 'react-native';
export const main = StyleSheet.create({
  helloWorldTextStyle: {
    fontFamily: 'Arial',
    fontSize: 30,
    textAlignVertical: 'center',
    textAlign: 'center',
  },
});
